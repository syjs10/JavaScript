function num() {
      var age = document.getElementById("age").value;
      if (age == "" || isNaN(age)) {
            window.alert('不是数字');

      }
      document.write(Date());
      console.log("hello world");
}
